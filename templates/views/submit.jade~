extends ../layouts/default

block intro
	.container
		h1 Submit a link, article or suggestion for inclusion
		
block content
	.container
		if enquirySubmitted
			h3 Thanks for your submission! One of our editors may get back to you soon.
		else
			h3 We like community submissions. Please paste a link or text in the field below.
			.row: .col-sm-8.col-md-6
				form(method='post')
					input(type='hidden', name='action', value='submit')
					.form-group(class=validationErrors.name ? 'has-error' : null)
						label Name
						input(type='text', name='name.full', value=formData['name.full'], placeholder='(or Anonymous)').form-control
					.form-group(class=validationErrors.email ? 'has-error' : null)
						label Email
						input(type='email', name='email', value=formData.email, placeholder='(valid email required)').form-control
					.form-group
						label Phone
						input(type='text', name='phone', value=formData.phone, placeholder='(optional)').form-control
					.form-group(class=validationErrors.message ? 'has-error' : null)
						label Submission
						textarea(name='message', placeholder='Put your link, suggestion or article here' rows=7).form-control= formData.message
					p By sending this to us to publish, you confirm that you have all necessary rights to do so and that we may include it at DailyInteresting.com. Please also see the full
						a(href="http://localhost:3000/a/post/terms") terms.
					.form-actions
						button(type='submit').btn.btn-primary Send
